package com.tech.test.infrastructure.controller.configuration

object UrlMapping {
    private const val API = "/api"
    private const val V1 = "/v1"
    private const val PREFIX = API + V1

    const val CITY = "$PREFIX/cities"
}